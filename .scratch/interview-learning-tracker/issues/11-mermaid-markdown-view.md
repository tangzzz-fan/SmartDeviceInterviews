# T11 — Mermaid + 统一 MarkdownView

**What to build:** Shared Markdown renderer with Mermaid code-fence support; use it on question / whiteboard / STAR / English pages. Add one mermaid diagram to 白板 1 for verification.

**Blocked by:** T05 (whiteboards exist)

**Status:** done

## Acceptance criteria

- [x] `MarkdownView` + `MermaidBlock` exist
- [x] Question / whiteboard / STAR / English use `MarkdownView`
- [x] `npm run build` passes
- [x] 白板 1 body includes a mermaid state diagram after reimport
