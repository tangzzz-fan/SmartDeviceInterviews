# MIT · Python 迁移（占位）

源仓库 `MIT-Migration/MIT-Python-Migration/` 目前只有目录占位（`.gitkeep`），尚无课程正文。

待上游补齐后，将 MD 放入本目录并重新运行：

```bash
cd backend && source .venv/bin/activate && python -m app.import_content
```
