# T7-03 批改与判定：LLM API 客户端与提示工程实践（示例）

> **示例文件**：演示「教练按密卷判分」长什么样（基于一份模拟学员作答），用于格式与判定标准参考，**不是真实产物**。
> 判定依据：[[T7-01-教练密卷-LLM API客户端与提示工程实践.md|T7 密卷]]。

## 一、学员作答摘要（模拟）

- C2：错误四类里把「上下文长度」漏了，重试映射只写了「出错就重试」。
- C3：指数退避讲清，但 429 的 `Retry-After` 优先级没提，重试预算概念缺失。
- C4：把 Semaphore 限流与连接池复用混为一谈。
- Q1：mock 服务器省略了（直接调假地址），超时路径没实测。
- Q3：熔断三态讲了 closed/open，**没有半开恢复**。

## 二、首轮判定

### 概念通道（6 题）

| 题 | 判定 | 判分点拆解 |
|----|------|-----------|
| C1 | 过关 | SDK vs httpx 直连判断标准给出，切换信号具体 |
| C2 | 半过 | 四类错误漏「上下文长度」；重试前提（可能不同结果）未讲 |
| C3 | 半过 | 退避+抖动讲了；Retry-After 优先级与重试预算缺失 |
| C4 | 未过 | 限流（并发在飞数）与连接池（底层连接）混为一谈 |
| C5 | 过关 | 消息职责边界 + token 预算公式正确 |
| C6 | 过关 | 结构化输出方法谱系 + 校验重试必要性讲清 |

### 代码通道（4 题）

| 题 | 判定 | 判分点拆解 |
|----|------|-----------|
| Q1 | 半过 | 客户端骨架对；mock 服务器省略、超时路径未实测 |
| Q2 | 过关 | 退避+抖动+Retry-After 实跑序列正确 |
| Q3 | 半过 | 熔断打开生效；半开恢复缺失 |
| Q4 | 过关 | 非法 JSON 被重试消费，上限固定 |

### 首轮总判定：**未过关**（概念 3过2半1未，代码 2过2半）

## 三、复攻记录

| 题 | 提示级别 | 记录 | 销账 |
|----|---------|------|------|
| C2 | 中 | 「重试的前提是什么——重试会有不同结果吗？哪类错误重试不会有？」 | ✅ 补上下文长度=确定性失败 |
| C3 | 中 | 「服务器给你的精确等待值，和本地估算谁优先？」 | ✅ Retry-After 优先 + 预算 |
| C4 | 强 | 「Semaphore 限的是同时在飞的请求，连接池限的是底层连接——两件事？」 | ✅ 三层模型 |
| Q1 | 无（自查） | 回看密卷评分要点 | ✅ 补本地 mock 服务器 + 超时实测 |
| Q3 | 中 | 「熔断打开后，恢复靠什么机制？」 | ✅ 半开试探 + 恢复 |

## 四、终审判定

复攻后概念 6 题全过、代码 4 题全过。**T7 过关。**

## 五、遗留观察项（带入后续主题）

- 熔断阈值（连续失败 N 次/窗口）在 T8/T9 不涉及，转项目线复查。
- 「重试预算」习惯在 T8 检索重试场景沿用。
