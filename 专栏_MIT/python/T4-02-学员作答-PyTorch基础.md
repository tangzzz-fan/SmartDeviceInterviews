# T4-02 学员作答：PyTorch 基础（训练闭环）

> 学员 agent 作答稿（闭卷，按 [[01-学员人设卡.md|人设卡]] 如实发挥）。自评三档：**有把握/ 半懂半猜 / 纯猜**。
> 代码一律 `.venv/bin/python` 实跑（torch 2.13.0），输出贴于第四节；撞墙记录在代码文件头。

## 〇、五模型自推（先写再对本）

自推版本：① Tensor 就是带梯度的 numpy 数组，requires_grad 开了就能 backward；② 图……forward 的时候顺手建的？backward 用完就没了；③ zero_grad 是训练模板里的一步，不清零会「有问题」；④ nn.Module 把层装起来，parameters() 能把参数拿出来给优化器；⑤ 训练循环五步我背得出：forward、loss、zero_grad、backward、step。

对教练版差异：M1 我完全没有「记账本/图上记边」这层——我把 autograd 当黑盒魔法；M2「现搭现拆」说不出来，我只知道「不能 backward 两次」这条规矩不知道为什么；M3 我知道要 zero_grad 但说不出累加是 feature；M4 收集机制我是纯黑盒；M5 我是模板记忆，「去掉会怎样」现场才推。**人设卡 T4 四条预期漏洞本轮全部命中，一条没跑掉。**

## 一、概念口述通道

**C1（两次 backward 与累加）**：① 我知道结果：grad 会累加——教程里反复强调 zero_grad 就是因为不清零会「越攒越大」。② 但「为什么选累加不选覆盖」我答不利索：现场想，可能是为了多个 loss 分开 backward？梯度累积这个词我听过但没用过。引擎视角「autograd 不知道你会调几次」这层是写完上面那句才反推出来的。自评：**半懂半猜**（现象熟、用途生）。

**C2（nn.Module 三连）**：① 我知道要走 `model(x)`，但理由是「教程都这么写」；`__call__` 里管 hook 这层我不知道——我以为它就是个语法糖。② 普通 tensor 会不会被收集……我猜不会？Parameter 和 tensor 的差别我只答得出「Parameter 默认要梯度」。③ 递归收集靠什么机制我只能猜「Module 内部有个列表存着」。自评：**纯猜**（人设卡原话：讲不清，实测确实讲不清）。

**C3（计算图不是函数调用链）**：① 我的第一反应确实是「backward 沿调用栈往回走」——写出来自己都觉得不对：函数早返回了，中间值在哪？② 节点是什么我答不出，「张量挂 grad_fn」这个说法我是打印过 loss.grad_fn 见过字样，但没建立心智。③ 第二次 backward 报错我见过，「图被释放了」能说到这层，retain_graph 没用过。自评：**半懂半猜**（旧心智在场，新心智碎片）。

**C4（view 与 CoW）**：① b = a.view 后改 b——说实话我第一反应是 CoW：写的时候应该会复制一份吧？Q3 实验③跑完 data_ptr 相同才接受「无拷贝共享」。② 梯度回流我不知道，实验③打印出 a.grad = 2a 才见到。③ detach 我知道「切断梯度」这个用途但没讲过它和 view 是两个正交维度（存储 vs 图）。自评：**半懂半猜**（CoW 直觉当场被实验纠正，记录在案）。

**C5（in-place 三连）**：① 版本号机制我完全不知道——人设卡说得对，我原本不知道 in-place 会破坏计算图。Q3 撞了三堵墙（叶上不炸、backward 前不炸、sum 不保存也不炸）才把触发条件摸出来：图建好了、被保存的中间量被就地改、还要再反传。② optimizer 里 in-place 安全我只能说到「更新发生在 backward 之后」。③ loss.item() 和存张量的区别：我平时都用 item()，理由是「拿个数」，显存泄漏这层是做了才知道。自评：**纯猜**（全靠 Q3 实验现场重建）。

**C6（五步循环去掉会怎样）**：① 去掉 zero_grad：梯度累加 → 等效学习率爆炸，这个能答（Q3 实验①就是证据）。② 去掉 backward 直接 step：首步 grad 是 None 会报错吧？之后用旧梯度——方向不对，现场推出来的。③ step 在 backward 前：同上，滞后一步用旧梯度。④ 拿中间激活 backward：「优化的就不是 loss 了」——这问我在 Swift 里类比过「断言测错了属性」，方向能说到。自评：**半懂半猜**（①稳，②③④现场推）。

## 二、争议焦点

**F1（动静图）**：动态图调试友好我深有体会（print 到哪看到哪）；代价我只答得出「每次建图有开销」。静态图占优的场景——部署端固定形状推理？torch.compile 我听过名字，「动态图 trace 成静态图再优化」这层是说 F1 时现查现拼的。自评：**半懂半猜**。

**F2（view vs clone）**：跑完 Q3 我的分界线：只读/形状变换用 view；要独立写入、要把数带出训练上下文（日志、返回值）用 clone/detach。用 Swift 类比就是「这块内存的所有权归谁」——CoW 帮不上忙因为 torch 根本不拷贝。自评：**半懂半猜**（答案新鲜出炉，还没长成肌肉）。

**F3（累加 bug 或 feature）**：我站 feature：Q3 实验①的累加语义很干净，梯度累积（显存不够攒 micro-batch）是真实用法。说 zero_grad 是设计失误的合理之处我想到一半：默认值之争——如果 step 后自动清零，攒梯度就得换写法。自评：**半懂半猜**。

## 三、自评汇总

概念：无一项有把握——C2/C5 纯猜，C1/C3/C4/C6/F1/F2/F3 半懂半猜。本轮与 T2/T3 的病型不同：**不是「会算无直觉」，是「会跑无机制」**——模板肌肉记忆全在，机制层大面积空白。代码四题每题都撞了真墙（共 8 处），墙的位置和人设卡的预期漏洞高度重合。

## 四、代码任务通道（实跑输出）

**Q1 纯 numpy 手推反向传播**（`代码/T4/Q1-纯numpy手推反向传播.py`）：

```
loss = 1.664085
W1[0, 0]: 手写 = -0.00275925  数值 = -0.00275925  相对误差 = 6.10e-12
……（9 个抽查点全部列出）
最大相对误差 = 2.47e-10
全部断言通过：手写梯度与数值梯度一致 ✓
```

撞墙 1 处：第一版忘乘 ReLU 门，首跑 W1/b1 抽查点误差 5.13e-03 ~ 2.80e-01、W2/b2 全对到 1e-10——断在哪一环清清楚楚（门在 W2 下游）。修复后最大误差 2.47e-10。

**Q2 torch 训练循环迁移与对拍**（`代码/T4/Q2-torch训练循环迁移与对拍.py`）：

```
dW1: torch vs 手写 最大相对误差 = 5.55e-17
…… db1/db2 = 1.11e-16，dW2 = 1.11e-16
对拍最大相对误差 = 1.11e-16
梯度对拍通过：autograd 与手推反向传播一致 ✓
训练 loss：首步 1.664085 → 末步 0.029748
```

撞墙 2 处：墙1 nn.Linear 的 weight 存 (out, in) 转置布局，直接搬 W1 报 `The size of tensor a (4) must match the size of tensor b (8)`；墙2 numpy float64 进 torch 与 float32 参数 dtype 不匹配报 `mat1 and mat2 must have the same dtype, but got Double and Float`——Swift 编译期能拦的，torch 拖到运行时。

**Q3 梯度诊断工具箱**（`代码/T4/Q3-梯度诊断工具箱.py`）：四实验全部复现：

```
实验① 轨迹 3→6→9 ✓（累加不是覆盖）
实验② RuntimeError: one of the variables needed for gradient computation has been
        modified by an inplace operation: [torch.FloatTensor [2]], which is output 0
        of Add, is at version 1; expected version 0 instead.
实验③ b.data_ptr() == a.data_ptr() → True；a.grad = [[2,4],[6,8]] = 2a ✓
实验④ 深度 2→4→6→8→10，‖∂loss/∂x‖ 每档 ×5.06（=1.5⁴）✓
```

撞墙 4 处（本轮最密集）：叶上 in-place 不炸 → backward 之前 in-place 也不炸 → z=y.sum() 还是不炸（sum 反向不保存 y！）→ 换 `(y**2).sum()` 才触发版本检查。触发条件三连：**图已建好 + 被保存的中间量就地改 + 还要再反传**。外加实验①我预期「覆盖」被实测纠正。

**Q4 Dataset/DataLoader 管道**（`代码/T4/Q4-Dataset与DataLoader管道.py`）：

```
判据①：seed=42 两遍顺序一致、seed=7 不同 ✓（共 8 个 batch，末批 4 个样本）
判据②：逐 batch 加权平均 = 0.7180694867，全量前向 = 0.7180694867  对拍一致 ✓
训练完成：loss 0.702476 → 0.653042（5 epoch）✓
```

撞墙 1 处（两级翻车）：对拍判据先栽在「简单平均」（0.7170 vs 0.7181），改成按样本数加权**还是不对**（0.7162 vs 0.7181）——因为加权 loss 的 batch 分母是 Σw 不是 n；终修按 ws.sum() 加权后严格相等到 1e-12。教训：**「平均的平均」先把每份的分子分母写出来再合并**。

撞墙总计：Q1 一处 + Q2 两处 + Q3 四处 + Q4 一处 = **8 处**，全部有真实报错/数值留档。人设卡 T4 预期漏洞命中清单：计算图心智缺失（C3/Q3 墙）、CoW 直觉套 view（C4/Q3 实验③）、in-place 不知道（C5/Q3 四墙中三堵）、nn.Module 讲不清（C2/Q2 墙1）。
