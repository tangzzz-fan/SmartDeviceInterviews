# 规范答案库：Nirva Founding iOS Engineer 面试题库

> 用法：P0 题可脱稿复述；每题答案控制在 1.5–2 分钟。标注「候选版」的章节由资深候选人 Agent 作答后评审并入。

## A. iOS 核心（P0）

### A1. Swift 并发：async/await 相比 GCD 解决了什么？actor 解决什么？

**答案**：

GCD 的问题是异步逻辑被拆散成回调闭包嵌套，错误处理和取消都很难做，线程安全完全靠程序员自觉（锁、串行队列）。async/await 把异步代码线性化，让编译器参与生命周期管理，配合结构化并发（TaskGroup、withTaskGroup）能清晰表达父子任务和取消传播。

actor 解决的是「共享可变状态」的并发安全：编译器保证对 actor 内部状态的访问串行化，把数据竞争从运行时问题变成编译期问题。比如 BLE 管理器维护连接状态、待确认包队列，多线程回调同时写就会竞争，用 `actor BleManager` 或 `@MainActor` 把所有状态操作串到一个执行域，就不需要手工加锁。

追问应对：`Task.detached` 不继承外层上下文；`Sendable` 是跨并发域传值的编译期约束；`@unchecked Sendable` 表示自己负责安全性，仅在封装好线程安全原语时使用。

### A2. ARC 原理与循环引用，怎么定位？

**答案**：

ARC 在编译期插入 retain/release，对象强引用计数归零时释放，确定性、无全局暂停，和 GC 不同。循环引用最常见的三类：delegate 被强持有（应 weak）、block 捕获 self（用 `[weak self]`）、Timer/DisplayLink 强持有 target 且 target 又持有 timer。

定位用两步：先复现，再用 Xcode Memory Graph 看对象间的引用环，能直接看到「谁持有谁」；Leaks 模板负责找泄漏对象。Allocations 看内存持续增长判断是否有隐性泄漏。验证释放写 deinit 打印，配合 weak self 后确认对象释放。

追问应对：`unowned` 在对象提前释放时会野指针崩溃，除非能保证生命周期更长，否则优先 weak；Swift 中闭包捕获列表、属性包装器也可能制造环。

### A3. App 从启动到首帧经历了什么？如何优化启动？

**答案**：

链路分两段：pre-main 和 main 之后。pre-main 包括 dyld 加载动态库、rebase/bind 修正、执行 +load 和 C++ 构造器；main 之后是 didFinishLaunching、首屏 UI 构建到首帧渲染。

优化手段按收益排序：pre-main 减少动态库数量（能静态就静态）、去掉 +load（改用 +initialize/懒加载）、减少 ObjC 元数据；main 之后把启动任务分级——首帧必需的任务留在同步路径，非关键的（埋点 SDK、推送注册、日志）全部异步化/延迟化，主线程避免 I/O 和大型解码。

指标要埋：pre-main 时长、didFinishLaunching 到首帧；真机 release 基线，CI 上做回归监控。我做过的一次：pre-main 从 450ms 降到 180ms（减库+去 +load），整体冷启从 2.8s 到 1.2s。

追问应对：首帧后仍有可感知白屏，要考虑首屏数据预取与骨架屏；SwiftUI 首帧与 UIKit 优化点不同。

### A4. 网络层怎么设计？

**答案**：

分层：APIClient（URLSession 封装）→ 路由/请求模型 → Codable 编解码 → 错误处理与重试 → 缓存 → 业务仓库（Repository）向上提供数据。每层职责单一，方便替换和测试。

关键设计：重试用指数退避加抖动（1s/2s/4s 封顶 3 次），只重试幂等请求，5xx 和网络错误区分处理；超时分请求超时和资源超时；GET 走缓存（ETag/版本号），写操作进离线队列；NWPathMonitor 监听网络恢复后重放队列，写请求带幂等键防止重复提交。

并发细节：token 过期刷新要做互斥，只允许一个刷新请求，其余等待；请求与页面生命周期绑定，离开页面取消，避免回调操作已释放对象。

追问应对：大文件下载用 background session + 断点续传，与普通请求分开管理。

### A5. 存储怎么选型？

**答案**：

按数据形态选：结构化关系数据用 Core Data/SwiftData（模型迁移、关系、fetched results）；查询复杂或库大用 SQLite（工程上常用 GRDB）；可穿戴健康数据这种时序数据，我倾向 GRDB 按天分表 + 增量同步 + 定期清理，时间戳对齐方便对账；固件包、音频这类大文件走文件系统 + 下载管理 + 校验，不塞数据库；敏感信息进 Keychain；轻量配置用 UserDefaults。

缓存分级：NSCache 内存 + 磁盘缓存（图片走 ImageIO downsampling）。

追问应对：Core Data 并发注意 context 类型（viewContext/backgroundContext perform）；模型升级做轻量迁移，复杂变更自定义 mapping；SwiftData 适合新项目但生态还在成熟。

### A6. 后台任务与数据同步怎么做？

**答案**：

现代方案是 BGTaskScheduler：BGAppRefreshTask 给一个较短窗口（约 15 分钟级别，由系统按使用习惯调度），BGProcessingTask 适合更重的批处理。注册在 didFinishLaunching，handleExpiration 里必须收尾，测试用 Xcode 的 Debug 菜单模拟触发。

它不能被当作定时器用，所以可穿戴 App 的可靠同步触发点是 push：收到通知再拉增量。数据同步只做增量对账和关键数据补传，绝不全量。App 被杀后没同步完的数据，靠下次启动补偿 + 服务端对账兜底。

追问应对：performFetch 已废弃；后台时间窗内做不完的任务要断点续传，记录进度而不是重来。

### A7. 讲讲你的架构演进经验。

**答案**：

我经历过 MVC 到 MVVM 的演进：业务膨胀后 ViewController 巨型化，先引入 ViewModel 让逻辑可测，再用 Coordinator 收敛导航。之后按 feature 拆模块（SPM），core 层（网络/存储/设计系统）下沉，依赖方向保持单向，避免模块环。

依赖注入用构造器注入优先，容器按需引入，不用 Service Locator 滥用。对状态复杂的地方（比如设备连接状态机 + 数据流），我评估过单向数据流（TCA 思想），但初创项目以简单可维护为先，只在真正需要的地方引入。

追问应对：模块化的代价是启动和包体积，收益是编译速度和边界清晰；循环依赖用协议下沉到 core 层破解。

### A8. 怎么把崩溃率做到 99.9%+？

**答案**：

四个环：收集、分析、预防、灰度。收集用 Crashlytics/Sentry + dSYM 符号化，并关联用户操作路径日志；分析做崩溃聚类，按版本/机型/系统过滤，定位回归 commit；预防是自动化测试（单元/UI/冒烟）+ 静态分析 + OOM 单独统计 + 卡顿监控（主线程 ping），因为 OOM 不算 crash 但体验更差；灰度分阶段发布（5%→20%→100%），异常即停，远程开关做功能级热修通道。

指标定义要清楚：crash-free sessions、OOM 率、卡顿率，各自设 SLA 和负责人。把「崩溃率」从结果指标变成「每个环都有可执行动作」的过程指标。

追问应对：线上崩溃复现不了就加强现场采集（设备状态、内存水位、最近操作），再按版本灰度二分。

### A9. 主线程卡顿怎么排查？

**答案**：

先用监控确认：RunLoop 观察（BeforeSources 与 AfterWaiting 间隔超过 50ms 记为卡顿）或子线程 ping 主线程，收集堆栈。定位用 Instruments Time Profiler 看主线程热点，配合 os_signpost 在关键阶段打点看耗时分布。

常见原因：主线程 I/O、大图解码（应 ImageIO downsampling）、复杂 Auto Layout、锁竞争/等待子线程结果、高频日志和字符串拼接。治理方向就是把这些移出主线程或预计算。

量化很重要：卡顿率从 x% 降到 y%，单次最长卡顿从多少降到多少，否则面试官会认为你在背方案。

追问应对：FPS 和卡顿不完全等价，掉帧 60→30 不等于卡顿；SwiftUI 的排查要用 Instruments 的 SwiftUI 模板看 body 计算。

### A10. Sendable 是什么？值语义和并发有什么关系？

**答案**：

Sendable 是编译器层面的并发安全检查：标了 Sendable 的类型可以安全地跨并发域传递。struct 在成员都 Sendable 时可自动满足；引用类型通常要不可变或 @unchecked Sendable。它把「并发安全」从运行时锁提升到编译期，比如 Task 闭包捕获、actor 方法参数都会检查。

值语义天然隔离：struct 传递即副本，但 COW（Array/String）共享底层缓冲，写时复制，所以多线程只读安全、写会触发复制。需要共享可变状态时，交给 actor 或锁保护的容器，而不是裸的 @unchecked Sendable。

追问应对：全局常量之所以安全是因为不可变；@MainActor 是把执行域约束到主线程，和 Sendable 解决的是两类问题。

## B. BLE 与软硬件联调（P0）

### B1. 介绍 Core Bluetooth 架构。

**答案**：

App 通常扮演 Central（CBCentralManager），设备扮演 Peripheral。GATT 模型上，Peripheral 暴露 Service，Service 里是 Characteristic（可读/可写/notify），Descriptor 描述特征（比如 CCCD 控制 notify 开关）。

关键状态机：CBCentralManager 的状态（unknown/resetting/unsupported/unauthorized/poweredOff/poweredOn），poweredOn 之后才能扫描和连接。流程是 scan → didDiscover → connect → didConnect → discoverServices → discoverCharacteristics → read/write/notify → 断连回调。

后台要点：扫描必须带 service UUID（不能空扫描 + duplicates），用 restoreIdentifier 注册状态恢复，连接丢失后系统可帮你重连。iOS 13+ 需要 NSBluetoothAlwaysUsageDescription 权限。

追问应对：notify 靠 CCCD 启用，App 订阅后外设才推送；iOS 不会自动重连所有外设，得自己实现恢复策略。

### B2. 扫描与连接的最佳实践？

**答案**：

扫描按 service UUID 过滤，这是后台扫描的硬性要求，也省电；前台可以开 allowDuplicates 做 RSSI 过滤。我会维护扫描定时器（比如 10 秒）避免无限扫描，扫描到目标后 stopScan 再 connect。

设备匹配不能只看名字：广播数据里的 manufacturer data 可以带协议号和序列号，同型号多台设备靠这个区分，RSSI 阈值（如 -70dBm 以上）防连到远处的设备。连接成功后保存 peripheral.identifier，下次直接重连，不用重新扫描。

去重防抖：同一设备短时间重复发现要合并；连接失败不要立刻重试，进入退避。

追问应对：identifier 在用户重装/重置后可能失效，要保留配对重绑定的兜底路径。

### B3. 连接参数怎么调优？

**答案**：

三个参数：connection interval（7.5ms–4s，两次连接事件间隔）、slave latency（外设可跳过的事件数）、supervision timeout（超过就判定断连，必须大于 interval 的合理倍数）。它们直接 tradeoff 吞吐、时延和功耗。

实时指令（控制命令）要低时延，interval 拉小；批量数据同步可以短时间拉小 interval 冲吞吐；空闲期调大 interval + 提高 slave latency 省电。我会按场景动态调参：空闲大 interval，同步期临时收紧，同步完再放开。

iOS 侧参数更新请求由外设发起，central 有裁决权，iOS 对参数有上限限制，要留余量。超时参数留大一点，避免弱信号下误判断连。

追问应对：iOS 实际吞吐受连接事件频率、ATT MTU、PHY（2M/CODED）共同限制，调参只解决一部分。

### B4. MTU 与吞吐怎么优化？

**答案**：

ATT MTU 默认 23 字节，有效载荷只有 20（减 3 字节 ATT 头），双方协商后 iOS 最高可到 517。每包有效载荷 = MTU - 3，这是大文件分块的依据。

写路径：write with response 每包等确认，慢但可靠；write without response 快，但外设 buffer 可能溢出，必须应用层流控（credit/ack 窗口，比如 20 包一确认）。读路径/推送：notify 吞吐高，配合外设 burst 和流控。

大文件传输：分块 + 序号 + CRC，支持断点续传（记录已传 offset），窗口批量确认。实测量级：2M PHY + 大 MTU 下 notify 能到数百 KB/s，具体受外设协议栈限制。

追问应对：压测方法——固定包长循环发，外设侧打印吞吐，对比不同 MTU/PHY 下的数据。

### B5. 设备与 App 状态同步怎么保证不丢不重？

**答案**：

核心机制：数据帧带序列号和时间戳，帧尾 CRC 校验；写路径要有确认；定期对账——设备返回本地最新 seq，App 从断点补拉，天然支持增量。

离线场景：设备侧把未同步数据持久化（环形缓冲/Flash），App 重连后按 seq 增量拉取并去重；App 侧把「哪些数据已确认」也持久化，App 被杀重启也能续传。

冲突处理要提前定规则：版本号、last-write-wins 或服务器裁决；健康数据一般以设备端记录为准，App 修改（比如编辑睡眠标签）走版本号合并。

追问应对：时钟不同步就设备用单调计数 + App 对时，或全部用设备相对时间戳，不做跨设备绝对时间比较。

### B6. 断连与异常恢复策略？

**答案**：

检测要双保险：didDisconnect(error) 回调 + 自定义 watchdog（连接在但长时间无数据，判定链路假死主动断开重建）。恢复走状态机：disconnected → connecting → connected → syncing。

重连策略：指数退避 + 抖动（1s/2s/4s/8s 封顶），全局统一退避防重连风暴；前台间隔短，后台/低电量拉长或暂停。重连后要恢复现场：重新服务发现、重订阅 notify、对账已同步数据、续传未完成任务（OTA/上传队列）；若系统把绑定清了，走重新 bonding 流程。

边界：用户主动断开不自动重连；连续失败后给用户明确入口（引导开蓝牙/重新配对）；App 被杀后依赖系统状态恢复或下次启动补偿同步。

追问应对：量化指标——重连成功率、平均恢复时长、首包恢复时间，要能讲出优化前后。

### B7. OTA 怎么设计？

**答案**：

流程：检查版本 → 下载固件包并校验（MD5/签名）→ BLE 分块传输（每包 CRC）→ 传输完成整体校验 → 写入 flash → 切换 bootloader → 重启 → 上报并验证新版本。

可靠性是重点：断点续传（记录已传 offset，重连后续传）；中途失败可重试；校验失败丢弃重下；固件用双 bank（A/B），升级失败自动回滚，避免变砖。开始前检查电量，低于阈值拒绝；传输期间防止设备关机。

业务侧：进度 UI（百分比 + 剩余时间）、前台后台策略、多设备批量升级队列（串行 + 每台状态机）。协议上要和硬件团队一起定 DFU 服务、包格式、版本协商，防止新固件改了协议导致兼容问题。

追问应对：变砖兜底——bootloader 有恢复模式、双 bank 回滚、电量保护三层。

### B8. 配对、绑定与安全怎么处理？

**答案**：

bonding 是配对后把密钥（LTK 等）存下来，重连不用重新配对；iOS 侧配对通常走系统弹框（或应用层发起），绑定信息系统存储。加密连接下，敏感 characteristic 的数据是加密传输的。

认证方式：legacy 配对不安全，MITM 风险高；secure 配对用 passkey/Numeric Comparison。健康数据这类敏感数据，除了 characteristic 加密，应用层再加密一层，云端传输走 TLS。

要处理 rebond 场景：重新绑定会清旧密钥，需要重新配对；还要处理「设备已被另一台手机绑定」的提示与解绑流程。

追问应对：iOS 自动弹配对框与应用控制的关系；App 自己保存 peripheral.identifier，重装系统后配对关系丢失要能重新引导。

### B9. BLE 功耗怎么优化？

**答案**：

四层：连接层——空闲期调大 interval + slave latency，数据同步期短暂收紧；扫描层——缩短扫描窗口、service 过滤、避免高频扫描，后台绝不空扫描；数据层——让外设聚合数据再 notify（批量推送），避免逐条推送，同样的数据量少唤醒几次；App 层——减少后台唤醒、合并 BGTask、不常开定位和网络。

测量闭环：Xcode Energy Log / Instruments Energy 看趋势，真机电流计测绝对功耗，优化前后对比，不然都是拍脑袋。

和固件侧分工：连接参数和推送策略必须双方一起定，App 侧能做的只是不添乱（少唤醒、少扫描、合理订阅）。

追问应对：举例量化——空闲连接电流从 xx mA 降到 xx mA，靠的是 interval 从 30ms 调到 300ms + latency 2。

## C. 软硬件联调（P1）

### C1. 怎么定义 App 与设备的通信协议？

**答题要点**：
* 帧格式最小集：magic + version + opcode + length + payload + CRC16；固定帧头方便解析与容错。
* 版本协商：连接后先交换协议版本，兼容旧固件（老固件用老字段，新字段走扩展）。
* 可靠性：seq + ack + 超时重传；命令响应配对（req/resp 对应）。
* 坑：大小端统一、字段对齐、长度字段与 payload 一致、保留位留扩展。
* 协作方式：协议文档双写（App 侧 + 固件侧）、联合评审、先用仿真工具对跑再上真机。

### C2. 联调出问题，怎么定位是 App 还是固件的锅？

**答题要点**：
* 分层隔离四步：1) 用 nRF Connect 直接对设备发包读特征，验证固件行为是否符合协议；2) 用桩设备/模拟器验证 App 侧解析与逻辑；3) BLE sniffer 抓包看 ATT 层真实收发（PacketLogger/Ellisys/Frontline）；4) 对齐固件 UART 日志与 App 日志时间线。
* 高频低级坑：MTU 分包不一致、CCCD 没订阅、字节序、长度字段错、服务发现缓存、时间戳单位。
* 输出规范：问题归属结论 + 复现步骤 + 证据（抓包/日志）+ 修复后验证。

### C3. 对 EVT / DVT / PVT 的理解？

**答题要点**：
* EVT：功能验证与设计确认阶段，软件重点是协议定型、功能联调，允许大改。
* DVT：设计定型，做可靠性/稳定性/OTA/功耗/认证测试，软件重点是磨稳定性、补自动化、修回归。
* PVT：量产验证，重点是产测（产线烧录/校准/功能测试）、量产问题响应、出货版本冻结。
* 加分讲法：举例说明某个阶段发现的问题倒逼了哪个设计变更。

### C4. 嵌入式基础了解多少？怎么协作？

**答题要点**：
* 知道 MCU 与 BLE SoC（nRF52/nRF53、STM32WB）、C/C++、UART/SPI/I2C、BLE 协议栈（Zephyr/SoftDevice）。
* 能读固件日志（UART 打印）、能理解固件侧 buffer/时序约束。
* 协作方式：接口契约文档、联调环境约定（设备型号/日志等级/复现条件）、问题分级（App/固件/协议/硬件）。

### C5. 音频采集或传感器数据处理经验？

**答题要点**：
* 链路：采样 → 缓冲 → 时间戳对齐 → 传输 → 本地存储 → 云端；音频/心率/IMU 各有吞吐与实时性要求。
* 音频流走 BLE 的挑战：MTU 与吞吐限制、缓冲抖动、掉包；端侧/云侧算法如何分工。
* 数据质量：与参考设备对比验证、异常值过滤、采样率与功耗平衡。

## D. 性能与基建（P1）

### D1. 启动优化你怎么量化？

**答题要点**：
* 链路：pre-main（dyld/rebase-bind/+load）→ main → 首帧；分别埋点。
* 手段：减动态库、去 +load、任务分级异步化、避免主线程 I/O。
* 数字模板：pre-main 450ms→180ms；冷启 2.8s→1.2s（注明机型与 release 基线）。
* 防回归：CI 每次合入跑启动基准测试。

### D2. 内存优化怎么做？

**答题要点**：
* 图片：ImageIO downsampling、解码异步、缓存分级（NSCache + 磁盘）。
* 峰值控制：分页加载、缓存上限与淘汰策略、内存警告时降级（清缓存/释放非关键）。
* 监控：memory footprint 上报、OOM 单独统计（区别于 crash）、Instruments Allocations 定位增长。

### D3. App 侧功耗怎么优化和测量？

**答题要点**：
* 三块大头：定位（按需 + 精度降级）、网络（合并请求/批量上报）、BLE（见 B9）。
* 后台：合并 BGTask、减少唤醒、低电量模式适配。
* 测量：Xcode Energy Log + 真机电流计，前后对比出数字。

### D4. 稳定性体系长什么样？

**答题要点**：
* 四环：收集（Crashlytics/Sentry + dSYM + 操作路径日志）→ 分析（聚类/版本过滤/回归定位）→ 灰度（分阶段 + 开关）→ 预防（测试/静态分析/卡顿与 OOM 监控）。
* 指标：crash-free sessions、OOM 率、卡顿率、各自 SLA。
* 关键认知：远程开关是止血通道，不是修复手段。

### D5. CI/CD 怎么从 0 建？

**答题要点**：
* 管道：lint → 单元测试 → UI 冒烟 → 签名（Match 管理证书）→ 打包 → TestFlight/内部分发，按分支/PR 触发。
* 发布：人工审批 + 灰度（分阶段上架）+ 回滚路径；构建产物可追溯（commit → 包号）。
* 注意点：签名与证书是最容易卡住的环节；测试要快，否则没人跑。

### D6. 自动化测试策略？

**答题要点**：
* 金字塔：大量单元测试 > 集成/UI 冒烟 > 少量端到端。
* 重点：BLE 状态机、同步引擎、协议解析这类核心逻辑必须单测（注入 mock peripheral / 内存队列），不依赖真机。
* 补充：UI 测试只保关键路径，快照测试防视觉回归；CI 集成 + 核心模块覆盖率目标（如 70%+）。

## E. 场景设计题（P1）

### E1. 设计可穿戴设备的数据同步架构。

**答题要点**：
* 链路：设备 → App（BLE 网关）→ 云。App 先落盘再上报，落盘优先保证不丢。
* BLE 侧：seq + CRC + 断点续传（见 B5）；本地时序库（GRDB 按天分表）。
* 云同步：增量、幂等键、冲突规则（设备为准/版本号）；失败用后台任务 + push 触发 + 下次启动对账补偿。
* 体验：实时指标（心率）前台实时，批量数据（睡眠）后台静默同步，功耗与实时性做 tradeoff。

### E2. 设计 BLE 自动重连状态机。

**答题要点**：
* 状态：idle / scanning / connecting / connected / syncing / backoff，事件驱动 + 定时器。
* 参数：退避（1/2/4/8s + 抖动）、前台/后台差异、最大重试后提示用户。
* 恢复现场：重订阅、对账、续传任务（见 B6）。
* 可测性：状态机纯逻辑可单测，BLE 调用层注入 mock。

### E3. 线上事故怎么处理？

**答题要点**：
* 响应顺序：判断影响面 → 止血（远程开关/下架/回滚）→ 定位（崩溃聚类、日志、版本二分）→ 修复 + 灰度发布 → 复盘。
* 复盘产出：根因文档、补测试、补监控告警、防再发清单。
* 加分：讲一个真实事故，重点在止血速度和复盘质量。

## F. 行为与动机（P0）

### F1. 讲一个你最有 ownership 的项目。

**答题要点**：用 STAR（见 04）。选「主动揽责/接手烂摊子/没人要求但你做了」的故事，必须有量化结果。

### F2. 你的 0→1 经历。

**答题要点**：从立项到上线的完整链路，突出「在模糊中定边界、独立决策、跨团队拉通」；没有完整 0→1 就讲「从 0 搭某个模块/基建」。

### F3. 需求很模糊，你怎么推进？

**答题要点**：先定目标与边界（问对问题）→ 出最小可验证方案 → 快速验证 → 透明同步；举例说明一次你主动把模糊需求变清晰的过程。

### F4. 跨团队/跨时区冲突怎么处理？

**答题要点**：对事不对人、用数据说话、先对齐目标再谈方案；举例：与固件团队在协议方案上的分歧如何收敛（原型验证代替争论）。

### F5. 讲一次失败与复盘。

**答题要点**：选真实失败，重点在「归因准确 + 机制改进」，避免甩锅也避免过度自责；结果是流程上防止再发。

### F6. 为什么加入初创硬件公司？

**答题要点**：三条线——对 AI 可穿戴方向的理解（软硬一体 + AI 是差异化）、对 0→1 ownership 的渴望、与硅谷团队协作的成长；避免只说「想冒险」。

### F7. AI-native：AI 怎么融入你的研发流程？举例。

**答题要点**：
* 至少 3 个具体案例，比如：AI 批量生成/维护单元测试；用 AI 解释 crash 日志与回溯定位；AI 审查 PR 与协议解析代码；用 AI 辅助固件/App 日志对照分析。
* 讲清「AI 负责什么、你负责什么」（判断力在你），并给产出数字（测试覆盖率、排障时间缩短）。
