# 强化策略：每个能力簇怎么深挖

> 原则：先能复述，再做 demo/写代码验证，最后用追问自测。每个模块标注「翻车点」。

## A. iOS 核心

**必掌握（P0）**
* Swift 并发：读官方《Swift Concurrency》要点；必须能现场画出「async/await 线性化回调、actor 保护共享状态」的例子（用 BLE 管理器举例最加分）。
* 内存：用 Xcode Memory Graph 实操一次定位循环引用；讲清 delegate/block/Timer 三类经典场景。
* 网络：画一版分层网络层（路由/请求/响应/错误/重试/缓存/离线队列）。
* 存储：对比 Core Data vs SwiftData vs GRDB，给出可穿戴时序数据场景的选型理由。

**深挖路径**：写一个 20 行左右的 demo（如 actor 保护一个连接状态机），跑起来验证；每讲一个机制配一个真实踩坑。

**翻车点**：只背名词不会举例；混淆 `Task.detached` 与 `Task`；说不清主线程隔离；内存只提 Leaks 不提 Memory Graph。

## B. BLE 全链路

**必掌握（P0）**
* 画出完整流程：扫描（过滤+RSSI）→ 连接 → 服务发现 → 订阅/收发 → 断连 → 退避重连 → 恢复现场。
* 参数三件套：connection interval / slave latency / supervision timeout，能讲清调参 tradeoff。
* 吞吐公式：ATT MTU 协商后每包有效载荷 = MTU - 3，分块 + ack 流控；能给出实测量级（notify 数百 KB/s 级别）。
* OTA：画时序图（下载校验 → 分块传输 → 每包校验 → 完成校验 → 切换 → 失败回滚），必须提到双 bank 与电量约束。

**深挖路径**：用 nRF Connect 连一个真实设备/模拟器，实操扫描、读特征、开 notify；如果手头有设备，验证断连重连。

**翻车点**：说不清 MTU 默认 23；把「通知」和「回调」混为一谈；OTA 只讲流程不讲中断恢复；重连策略没有退避与前后台差异。

## C. 软硬件联调

**必掌握（P1）**
* 协议设计最小集：magic + version + opcode + length + payload + crc；seq/ack/超时重传；大小端与字段对齐。
* 排障四步：1) 用 nRF Connect 直接对设备发包，验证固件侧；2) 用桩设备/模拟器验证 App 侧；3) BLE sniffer 抓 ATT 层；4) 对齐双方日志时间线。
* EVT/DVT/PVT：EVT 功能验证、DVT 设计定型+可靠性、PVT 量产验证；软件在每阶段的重点。

**翻车点**：把问题全推给固件；不会用 sniffer；对字节序/长度字段等低级错误没有意识。

## D. 性能与基建

**必掌握（P1）**
* 启动：pre-main 到首帧的链路；优化手段按「收益/风险」排序；指标埋点。
* 稳定性：收集（Crashlytics/Sentry + dSYM）→ 分析（聚类/符号化）→ 灰度 → 预防（测试+监控+开关）。
* CI/CD：fastlane 一条龙（测试→签名→打包→分发）；灰度发布与回滚。
* 功耗：定位/网络/BLE 三块；Energy Log 与真机实测。

**翻车点**：只有结论没有数字；把 Crashlytics 当全部稳定性方案；CI 只提 Jenkins 不提测试策略。

## E. 行为与 AI-native

**必掌握（P0）**
* 准备 6+ 个 STAR 故事（见 04），每个都有量化结果。
* AI-native 至少 3 个案例：AI 写测试/解释 crash/AI 评审/用 AI 加速联调日志分析等。

**翻车点**：故事没有结果数字；AI 案例只是「我用过 ChatGPT」而没有具体产出。

## F. 英文

**必掌握（P1）**
* 60 秒自我介绍背熟；一个技术故事用英文讲 5 分钟；daily sync 三类模板。

**翻车点**：中文思维直译；术语不统一（如 notify/characteristic 前后不一致）；反问环节没准备问题。
