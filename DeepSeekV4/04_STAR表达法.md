# STAR 故事库

> 用法：所有故事先用模板填真实经历（【】为占位），再压缩成 30 秒电梯版。行为题只讲 1.5–2 分钟，结果必须有数字。
> STAR = Situation（背景）· Task（任务）· Action（你具体做了什么）· Result（可量化结果 + 沉淀）。

## 模板结构

1. Situation：一句话背景（项目/团队/时间），交代为什么难。
2. Task：你的具体职责与目标（用一句话可验收的目标）。
3. Action：3–4 个动作，突出「你的决策与判断」，不是团队做了什么。
4. Result：数字结果 + 沉淀（流程/文档/机制），一句话收尾。

## S1. Ownership：接手线上稳定性差的烂摊子

**STAR 模板**：
* S：App 上线后 crash-free 只有 98.2%，差评集中在闪退，团队忙于新功能没人牵头。
* T：我把「稳定性专项」主动揽下来，目标 crash-free 提升到 99.7% 以上。
* A：1) 接入 Crashlytics + dSYM 自动符号化，按版本聚类定位 Top10；2) 锁定 3 个主要崩溃源（内存泄漏 + 空数据崩溃 + 旧版本兼容），逐一修复；3) 建立 OOM 与卡顿监控，补回归测试。
* R：三个月 crash-free 98.2% → 99.8%，差评率下降 40%，沉淀了稳定性巡检清单（每周看指标）。

**30 秒电梯版**：App 线上崩溃率 98.2%，没人牵头，我主动立了稳定性专项——先建崩溃聚类，再修 Top3 根因，三个月到 99.8%，并沉淀了每周巡检机制。

**可能的追问**：为什么是这 3 个根因？线上不能复现的崩溃怎么处理？（答：加现场采集 + 版本二分。）

## S2. 0→1：新产品从立项到上线

**STAR 模板**：
* S：公司从 0 做一款【可穿戴/智能硬件配套】App，团队只有我 + 1 名后端，无产品文档。
* T：负责 iOS 端全部技术决策与交付，3 个月配合硬件完成 EVT 联调并上线。
* A：1) 定义 MVP 边界，砍掉非核心功能；2) 定 BLE 协议与数据结构（与嵌入式团队对文档）；3) 搭工程骨架（模块化 + CI + 埋点）；4) 全程英文与硅谷团队同步进度。
* R：按节点上线，EVT 阶段联调问题清零率 x%，首版 NPS 【】；沉淀了协议文档与联调 checklist。

**30 秒电梯版**：从 0 做智能硬件配套 App，我定了 MVP 边界、BLE 协议和工程骨架，3 个月完成 EVT 联调并上线，全程用英文和硅谷团队同步。

**可能的追问**：最难的决策是什么？如果重来一次哪里会不同？

## S3. BLE 联调排障：断连与数据丢失

**STAR 模板**：
* S：EVT 阶段设备频繁断连、偶发数据丢失，固件说 App 问题，App 说是固件问题。
* T：24 小时内给出可复现结论并推动修复。
* A：1) 用 nRF Connect 直测设备，排除 App；2) BLE sniffer 抓包发现【MTU 分包/CCCD/超时参数】问题；3) 与固件工程师对齐日志时间线，确认根因在【固件 buffer/App 重连逻辑】；4) 双方各自修复并加联调回归用例。
* R：断连率从 x% 降到 y%，数据丢失率归零；沉淀「联调排障手册」并被团队沿用。

**30 秒电梯版**：EVT 阶段设备频繁断连，两边互相甩锅，我用 nRF Connect 和抓包把问题定位到固件 buffer，24 小时内出结论，双方修复后断连率归零。

**可能的追问**：抓包里你怎么看出是 buffer 溢出？如果协议本身有缺陷你会怎么改？

## S4. 性能优化：启动时间

**STAR 模板**：
* S：App 冷启 2.8s，首轮体验差评集中在「打开慢」。
* T：冷启优化到 1.5s 以内（iPhone 12 基线）。
* A：1) 拆分 pre-main/main 两段埋点；2) 减 3 个动态库、去 +load；3) 启动任务分级，非关键 SDK 全部异步/延迟；4) CI 加启动基准防回归。
* R：冷启 2.8s → 1.2s，卸载重装用户留存提升 x%。

**30 秒电梯版**：冷启 2.8s，我从 pre-main 开始拆指标——减库、去 +load、任务异步化，优化到 1.2s，并把启动测试挂进 CI 防回归。

**可能的追问**：pre-main 和 main 后的优化收益怎么分？为什么留 1.2s 不继续压？

## S5. OTA 从 0 设计

**STAR 模板**：
* S：硬件要量产，OTA 完全没有：固件更新靠返厂。
* T：设计并实现 OTA 方案，支持断点续传、失败回滚。
* A：1) 与固件团队定义 DFU 服务与包格式；2) App 实现分块传输 + 每包 CRC + 断点续传；3) 推动固件做双 bank 回滚；4) 加电量保护与批量升级队列。
* R：OTA 成功率 99.x%，返厂升级成本归零；量产工具链（产测）也复用了同一套协议。

**可能的追问**：断点续传的 offset 存在哪？升级到一半设备重启怎么处理？

## S6. 跨团队冲突：协议方案分歧

**STAR 模板**：
* S：与固件团队对「数据推送 vs 拉取」方案分歧，双方都认为自己的方案对，会议僵持。
* T：收敛分歧并定方案。
* A：1) 把两种方案各做 5 行伪代码 + 功耗/复杂度对比表；2) 用 demo 实测功耗与丢包；3) 用数据说服，并接受对方合理的部分（混合方案）；4) 把决策写进协议文档。
* R：方案 3 天定稿，后续无返工；和固件团队建立了「先数据后争论」的协作约定。

**可能的追问**：如果数据不足以决策你会怎么办？

## S7. 失败复盘：一次发布事故

**STAR 模板**：
* S：【一次发布】导致线上【崩溃/数据异常】，影响用户 x%。
* T：止血 + 复盘，确保不再犯。
* A：1) 先远程开关/下架止血；2) 定位根因是【没覆盖的场景/测试缺失】；3) 补测试与监控告警；4) 发布流程加灰度门槛。
* R：影响恢复时间 x 分钟；同类事故 6 个月未再发生。

**可能的追问**：你自己在事故中的责任是什么？

## S8. AI-native：AI 融入研发流程

**STAR 模板**：
* S：核心模块测试覆盖率只有 30%，排障靠人肉翻日志。
* T：把 AI 深度融入测试与排障，产出可量化收益。
* A：1) 用 AI 批量生成 BLE 状态机与协议解析的单元测试，我负责审边界与正确性；2) 联调日志交给 AI 对照分析，标注时间线差异；3) AI 做 PR 初审（协议/并发/内存风险点）；4) 沉淀团队 AI 使用规范（哪些能信、哪些必须人审）。
* R：核心覆盖率 30% → 78%，联调排障时间缩短约 40%；AI 产出代码缺陷率【】。

**可能的追问**：AI 生成测试你如何保证质量？如果 AI 建议错了你会怎么发现？

## 使用提醒

* 【】内容必须替换成真实经历与真实数字，面试官一定会追问细节，编造会被识破。
* 每个故事准备 3 个「可能的追问」答案。
* 数字宁可用区间（「提升约 40%」）也不要虚构精确值。
